import 'package:flutter/cupertino.dart';
import 'package:walewein/shared/constants.dart';

// Sized boxes
const defaultHeightSizedBox = SizedBox(height: kDefaultPadding);
const defaultHalfHeightSizedBox = SizedBox(height: kDefaultPadding / 2);
const defaultWidthSizedBox = SizedBox(width: kDefaultPadding);
const defaultHalfWidthSizedBox = SizedBox(width: kDefaultPadding / 2);

// Border radii
final defaultButtonBorderRadius = BorderRadius.circular(7);
